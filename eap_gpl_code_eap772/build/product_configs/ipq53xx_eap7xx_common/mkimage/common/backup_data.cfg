[backup_data]
# Volume mode (other option is static)
mode=ubi
# Source image
image=backup_data.ubifs
# Volume ID in UBI image
vol_id=0
# Allow for dynamic resize
vol_type=dynamic
# Volume name
vol_name=ubi_backup_data
vol_flags=autoresize
